// Database module stub - in-memory only mode
// All database functionality has been disabled per user request.

function getConnectionStatus() {
  return false;
}

module.exports = { getConnectionStatus };
